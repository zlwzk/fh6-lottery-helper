# FH6 抽奖助手 v1.2.0

> 把《极限竞速：地平线》里攒下来的抽奖次数一次性刷完，不用手点几千下。
> 这一版把「按固定流程按键」换成了**看画面办事**，并且会告诉你一共赚了多少 CR。

**下载：** 见本页下方的 `FH6LotteryHelper.exe`（单文件，双击即可运行，无需安装 Python）。

---

## 本版更新（v1.2.0）

### 不再背固定流程，改成「看画面办事」
以前是一套写死的按键顺序往下走，游戏多弹一步、界面慢一拍就会错位。现在每一轮只做三件事：

```
看屏幕 → 认清现在停在哪个界面 → 把画面上的数字读出来记账 → 按这个界面该按的键
```

每个界面有自己的处理函数（抽奖界面 / 新车 / 已拥有车辆 / 出售确认 / 结束 / 被挡住），互相不干涉。
所以你先手动抽到一半再点开始，它也能接上；中途多出一个确认框，它自己会跟上。
每一步都由画面决定，不要求你按游戏的固定顺序操作。

### 新增：自动读出「这一抽抽到了什么」
- 转盘的奖励格会**自动定位高亮的那一格**（白底那一块），只对那一小块做识别，又快又准；
- 是 **CR** 就累加进「抽奖所得」，是**车**就记一笔并交给「已拥有车辆」策略处理；
- **Super Wheelspin 一次给三份**也没问题：一列一列的高亮格全都会读出来一起统计；
- **一次抽奖只记一次账**：结果画面停在那里再久，也不会把同一份奖励算成好几份；
- 不放心可以先点「测试识别当前画面」，日志里会逐格告诉你读到了什么。

### 新增：赚钱账本 + 计时器
- **抽奖所得 CR**：直接从转盘抽到的钱；
- **出售所得 CR**：抽到重复车并选择「出售」时，读出价格累加（价格就写在选项那一行，不用等确认界面）；
- **合计收益**：两者相加；
- **总耗时**：从点开始到停止，概览页与悬浮窗实时显示。收工时日志里再给一句小结——
  「共抽奖 N 次，总耗时 X；抽到车辆 Y 辆、CR Z；卖出 M 台共 W CR；合计收益 ... CR」。

### 重复车怎么处理，提前定好
「添加至车库 / 送礼 / 出售」三选一，在「流程」页设一次，之后每台重复车都照这个来。
选「出售」时会先把价格读出来记账，再按确认；读不出价格而你又开着保护，就直接停机，
避免认错界面把车卖掉。

### 剩余抽奖次数
- 那一栏是滚动数字（一屏可能同时看到 997 / 998 / 999），默认**取最大的那个**：
  顶多多抽一次，不会提前停；也可以改成取最小或取读到的第一个；
- 读到 0 就自动收工。

### 顺带
- 悬浮窗统计格重排：抽到车 / 抽奖所得 / 出售所得 / 合计 CR / 耗时；
- 自检从 9 项加到 10 项，新增「抽奖结果识别」，用合成图验证高亮格定位与钱 / 车判定；
- 概览页的「三分钟上手」按新的设置顺序重写，并补上「统计出来的 CR 一直是 0」的排查步骤。

### 上一版（v1.1.1）的修复也都在
OCR 回包缺少请求序号、导致关键词识别整体失效（v1.1.0 引入）已修复；
重复静止画面等 2.5 秒重试一次；卡住计时与重试节流拆成两个计时器；
游戏不在前台时不再满速空转。

---

## 它是怎么工作的

只做两件事，**绝不碰游戏本体**：

1. **看屏幕** —— 截屏 + 模板匹配 / Windows 系统自带 OCR，判断当前停在哪个界面；
2. **动键盘** —— 用 `SendInput` / `PostMessage` 模拟按键。

不读游戏内存、不注入 DLL、不改游戏文件、不抓网络包。
换句话说，它做的是「把手动操作自动化」，能力上限低一些，但安全边界很清楚。

---

## 完整功能

### 智能识别模式
- 截取游戏画面，用**模板匹配 + OCR** 判断界面角色：抽奖主界面 / 结果·已拥有 / 结果·新车 / 出售价格 / 确认弹窗 / 抽奖结束 / 阻挡界面
- 抽到重复车时自动按你选的策略处理：**加入车库**（Enter）、**送礼**（↓ + Enter）、**出售赚 CR**（↓↓ + Enter）
- 可开启「读取游戏内剩余抽奖次数」，或设置「抽够 N 次自动停止」

### 节奏宏模式
- 识别不准时的保底方案：把「按键 / 等待 / 鼠标点击」排成循环，助手照着按
- 点击坐标以游戏窗口为原点，窗口挪位置也不会点偏
- 内置「标准循环」「快速连按」两套预设

### 模板库
- 按 `F9` 直接在游戏里**冻结画面框选**保存模板，比来回切窗口快得多
- 可绑定语义角色、单独调匹配阈值、**限定匹配区域**，支持缩略图预览与增删改
- 换分辨率后自动按比例适配，不用重录

### 识别与调试
- 实时显示匹配命中位置、相似度、OCR 文本，方便调阈值、换模板
- 可框选 OCR 区域，避免整屏识别拖慢速度
- 识别失败自动留存现场截图，可一键打开目录

### 悬浮窗
- 置顶小窗显示进度与统计，默认**鼠标穿透**（不挡游戏操作），`F10` 切换锁定状态
- 可调不透明度与缩放

### 安全保护
- **急停**：鼠标快速甩到屏幕左上角立即停止
- **前台校验**：游戏不在前台时完全不发按键，防止误按到别的程序
- 连续识别失败超过上限自动停下并提示补模板
- 画面长时间无变化判定卡住并停止
- 出售价格读不出时可选直接停机，避免误卖
- 悬浮窗支持点击穿透，不会误触

### 完成后的操作
- 抽完自动：关机（带倒计时与取消）/ 关闭游戏 / 启动指定程序 / 播放提示音
- 每项独立开关，默认全关

### 全局热键
| 按键 | 作用 |
| --- | --- |
| `F7` | 开始抽奖 |
| `F8` | 停止抽奖 |
| `F11` | 暂停 / 继续 |
| `F9` | 捕获模板（冻结画面框选） |
| `F10` | 显示 / 隐藏悬浮窗 |

热键全部可改。注册失败会明确提示（通常是输入法、显卡驱动面板、录屏软件占用）。

---

## 系统要求

- Windows 10 / 11（64 位）
- 使用 OCR 功能需要系统装有对应语言的**光学字符识别**组件
  （设置 → 时间和语言 → 语言 → 中文/英文 → 语言选项 → **光学字符识别**）
- 用源码运行需要 Python 3.10+ 与 `pip install -r requirements.txt`

---

## 快速上手

1. 进游戏，停在抽奖界面（能看到「抽奖」按钮的那个）
2. 助手「设置 → 游戏窗口」填关键字（默认 `forza`），点「自动识别」
3. 「模板库」或按 `F9` 补两个关键模板就够用：
   - 抽奖主界面 → 角色 `ready`
   - 结果界面「已拥有」→ 角色 `result_owned`
4. 给这两个模板各点一次「匹配区域 → 框选匹配区域」，把范围收窄到按钮附近（很关键）
5. 「流程（智能识别）」页选好已拥有时的处理方式，等待时间按自己电脑微调
6. 按 `F7` 开始，`F11` 暂停，`F8` 停止

---

## 数据存放位置

全部在 `%APPDATA%\FH6LotteryHelper\`：配置、模板库、日志、临时图、失败现场截图。

**刻意不写进游戏目录**，这样覆盖安装或删除助手都不会丢数据，也不会在游戏目录里留垃圾。
界面中显示的路径都会折叠成 `%APPDATA%\...` / `%USERPROFILE%\...` 占位符，
日志与崩溃信息同样会脱敏，不会暴露本机用户名。

---

## 已知限制

- 游戏版本更新改了界面外观，模板会失效，需要重新截模板（分辨率变化已能自动适配）
- 「后台模式」（窗口消息投递）对部分游戏/部分按键无效，这是游戏自身限制；推荐用默认的全局模式并保持游戏在前台
- 模板与画面颜色差异较大时相似度会偏低，可到「设置」里适当降低全局阈值
- 本机若没装 Windows OCR 语言包，OCR 关键词识别不可用，模板匹配不受影响

---

## 免责声明

本项目仅供学习 Windows 自动化（截屏识别 + 模拟输入）使用。
请在符合游戏用户协议的前提下自行使用，因使用本工具产生的任何后果由使用者自负。
请控制使用频率，不要长时间挂机。
