# FH6 抽奖助手运行依赖（Windows 10/11，Python 3.10+）
# 安装：pip install -r requirements.txt
#
# 说明：
# - 界面用 PySide6（Qt6）
# - 截图用 mss（多屏/高 DPI 友好）
# - 识别用 OpenCV 模板匹配
# - OCR 直接调用 Windows 自带的 Windows.Media.Ocr，不需要额外安装 tesseract

PySide6>=6.6
opencv-python>=4.8
numpy>=1.24
mss>=9.0
pillow>=10.0
pyinstaller>=6.0   ; # 仅打包时需要
